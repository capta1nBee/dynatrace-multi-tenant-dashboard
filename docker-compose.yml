version: '3.8'

services:
  dynatrace-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dynatrace-multi-tenant-app
    ports:
      - "5173:5173"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    

